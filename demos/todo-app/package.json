{
  "name": "watson-todo-app",
  "version": "1.0.0",
  "description": "Multi-command TODO manager built with Watson",
  "type": "module",
  "scripts": {
    "dev": "bun run ./src/cli.ts",
    "build": "bun build ./src/cli.ts --compile --outfile dist/todo"
  }
}
