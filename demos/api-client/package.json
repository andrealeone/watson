{
  "name": "watson-api-client",
  "version": "1.0.0",
  "description": "Environment-aware API client CLI built with Watson",
  "type": "module",
  "scripts": {
    "dev": "bun run ./src/cli.ts",
    "build": "bun build ./src/cli.ts --compile --outfile dist/api"
  }
}
